// import { Project, projects } from "../../ProjectData";

type ProjectInfoProps = {
    currentProject: object;
};

const ProjectInfo = ({ currentProject }: ProjectInfoProps) => {
    // console.log('right here', currentProject)
  return (
    <div>
    
    </div>
  );
};

export default ProjectInfo;
